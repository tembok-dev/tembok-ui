{
  "name": "tembok-ui-draft",
  "private": true,
  "version": "0.0.0",
  "workspaces": ["packages/*"],
  "scripts": {
    "build": "pnpm -r --filter ./packages/** run build",
    "typecheck": "pnpm -r --filter ./packages/** run typecheck",
    "clean": "rimraf \"packages/*/dist\""
  },
  "devDependencies": {
    "rimraf": "^5.0.5",
    "tsup": "^8.0.1",
    "typescript": "^5.6.2"
  },
  "packageManager": "pnpm@9.x"
}
